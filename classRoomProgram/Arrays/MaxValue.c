#include <stdio.h>

int main(){
    int A[10], i, max;
    printf("Enter the number");
    for(i=0; i<10; i++){
        scanf("%d", &A[i]);
    }
    printf("The enterd Arrays is as");
        for(i=0; i<10; i++){
            if(max>A[i]);
            max=A[i];
    }
    return 0;
}